# Gradle properties pour ATOMIC FLIX Mobile
# Optimisations pour builds stables

# Android configuration
android.useAndroidX=true
android.enableJetifier=true

# Build optimization - KSP Memory Leak Fix (2024-2025)
# Solution: https://github.com/google/ksp/issues/2073
# Key: Avoid MaxMetaspaceSize limit (causes KSP OOM), use G1GC instead
org.gradle.jvmargs=-Xmx8g -Xms1g -Xss8m -XX:+UseG1GC -XX:SoftRefLRUPolicyMSPerMB=1 -XX:ReservedCodeCacheSize=256m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configureondemand=true
org.gradle.workers.max=4

# Kotlin daemon settings - must match main JVM settings
kotlin.daemon.jvmargs=-Xmx8g -XX:+UseG1GC -Xss8m

# Disable warnings and optimizations
android.enableR8=false
android.enableDesugaring=false
android.enableProguardInReleaseBuilds=false
android.useProguard=false

# New architecture disabled for stability
newArchEnabled=false
hermesEnabled=false

# Network and build fixes
systemProp.http.proxyHost=
systemProp.http.proxyPort=
systemProp.https.proxyHost=
systemProp.https.proxyPort=

# JAVA_HOME fix for builds
org.gradle.java.home=/usr/lib/jvm/java-17-openjdk
systemProp.java.home=/usr/lib/jvm/java-17-openjdk

# Additional Java environment fixes
systemProp.JAVA_HOME=/usr/lib/jvm/java-17-openjdk