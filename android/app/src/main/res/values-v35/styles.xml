<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Styles spécifiques pour Android 35+ -->
    <style name="AppTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Configuration pour Android 35 -->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
        <item name="android:enforceStatusBarContrast">false</item>
        <item name="android:enforceNavigationBarContrast">false</item>
        <!-- Désactive explicitement windowOptOutEdgeToEdgeEnforcement pour éviter l'erreur -->
        <!-- <item name="android:windowOptOutEdgeToEdgeEnforcement">false</item> -->
    </style>

    <!-- Theme edge-to-edge pour Android 35 -->
    <style name="AppTheme.EdgeToEdge" parent="AppTheme">
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowTranslucentNavigation">false</item>
        <item name="android:fitsSystemWindows">false</item>
    </style>
</resources>